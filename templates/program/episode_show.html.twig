{% extends 'base.html.twig' %}

{% block title %}Episode #{{ episode.number }}{% endblock %}

{% block body %}

<div class="media">
    <div class="media-body" >
        <h1 class="mt-0" style="border-bottom: 1px solid black; padding-bottom: 5px">{{ program.title }}</h1>
        <h3>Season {{ season.id }}</h3>
        <h4>Episode {{ episode.number }} : {{ episode.title }}</h4>
        <p>{{ episode.synopsis }}</p>
    </div>
    <a href="{{ path('program_season_show', { program: program.id , season: season.id}) }}">
        Retour à la liste des Episodes
    </a>
</div>

{% endblock %}
